import random
import sys
import os

# Tuples are very similar to lists
# Unlike Lists once created Tuples do not change

pi_tuple=(3,1,2,3,4,4,5,9)

new_tuple=list(pi_tuple) # Converts tuple to a list

#  len(tuple) [length of tuple]  min(tuple) [minimum of tuple] max(tuple) [maximum of tuple]

